# Lab 10 – Database Connectivity (ADO.NET)

This lab explores how to connect a Windows Forms application to a SQL Server database using ADO.NET. It demonstrates essential database operations, ADO.NET objects, and data handling techniques.

## 📌 Topics Covered
- Introduction to ADO.NET  
- Data Providers  
- SqlConnection  
- SqlCommand  
- SqlDataReader  
- DataSet  
- SqlDataAdapter  
- Creating a database in Visual Studio  

## 🧪 Exercises

### **Exercise 10.1 – CRUD Operations with ADO.NET**
Build a Windows Forms application that:
- Connects to a `StudentDB` database  
- Uses `SqlDataAdapter`, `SqlCommandBuilder`, and `DataSet`  
- Performs **Insert**, **Update**, and **Delete** operations  
- Displays all records in a `DataGridView`  
- Syncs UI changes with the database  

### **Exercise 10.2 – INNER JOIN + Filter Query**
Create a database connection and:
- Retrieve combined student and department data using `INNER JOIN`  
- Display results in a `DataGridView`  
- Provide a **"Retrieve By Last Name"** search feature  
- Use **parameterized queries** to prevent SQL injection  

## ✔️ Conclusion
This lab provided hands-on experience with ADO.NET by implementing database connectivity, executing SQL commands, working with DataSets, and displaying relational data in Windows Forms. It strengthened skills in building secure and functional data-driven applications.

